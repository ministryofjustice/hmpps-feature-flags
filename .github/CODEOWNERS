# Admins own everything by default
* @ministryofjustice/hmpps-feature-flag-admins

# Flag changes don't need a code owner review - the flag-approval workflow
# enforces the review policy for them (see README)
flags/

# access.yml defines writers and prod self-service, so changing it requires
# an admin review
flags/*/*/access.yml @ministryofjustice/hmpps-feature-flag-admins
